# SOYUL PORTFOLIO — DESIGN SYSTEM v1

인터뷰 2라운드로 확정된 결정 사항. 이 문서에 없는 결정은 아직 열려 있다.

---

## 0. 포지셔닝

> **혼자 끝까지 만드는 사람.**
> 브랜드를 세우고, 그걸 직접 웹으로 만들어 납품까지 합니다.

- 근거 A — 개인 작업 3개(바밤비 / 고요 / 열세달꽃)에서 컨셉·브랜딩·콘텐츠·웹을 혼자 완결했다.
- 근거 B — 실무 3사에서 키오스크·주문웹·메타버스까지 이종 환경을 구현했다. 이것이 "혼자 만든다"를 취미가 아닌 실무 역량으로 보증한다.
- 근거 C — 열세달꽃은 습작이 아니라 실제 클라이언트 납품이다. 개인 작업과 실무의 경계를 지운다.

카피 톤: 능력을 선언하지 않고 **결과물이 먼저 말하게 한다.** 사이트의 문장은 짧고 사실적으로.

---

## 1. 컨셉 — 암흑 보이드

우주를 **그리지 않는다.** 우주 **안에 둔다.**

- 별·성운·행성·궤도 그래픽 **없음**. 사용하는 것은 넷뿐: **암흑 / 스케일 / 부유감 / 좌표**.
- 화면의 유일한 색은 **작업물 이미지**다. UI는 전부 무채색이므로 9개 작업물의 색이 서로 싸우지 않는다.
- 히어로의 파티클 필드는 유지한다. 단 히어로에서만. 작업 구간으로 넘어가면 파티클은 정지하고(성능) 순수한 검정만 남는다.
- 섹션 전환은 기존 히어로의 **블랙홀 이탈**(축소 + 블러 + 페이드)을 계승한다. 이것이 이 사이트의 시그니처 모션이다.

## 2. 구조 — 풀스크린 챕터 스크롤

작업물 한 개가 화면 하나를 통째로 차지한다. 한 번에 하나의 천체만 마주친다.

```
HERO (고정, 블랙홀 이탈)
  ↓
WORK — 챕터 01 … 08  (각 100svh, 단일 흐름)
  ↓
ABOUT (경력 · 3사)
  ↓
CONTACT
```

- **단일 흐름 + 메타 표기**: 개인/실무를 섹션으로 가르지 않는다. 각 챕터에 `SELF-DIRECTED` / `RTBS` / `UTPLUS` 같은 소속과 연도, 역할을 모노스페이스로 작게 표기한다.
- **묶음 원칙** — 한 클라이언트·한 생태계는 목록에서 한 챕터, 상세 페이지 안에서 뚜렷한 하위 챕터로 분리한다.
  - `DITOLAND` = 웹 + 앱 + 근로복지공단 메타버스 (3)
  - `TOM N TOMS` = 주문웹 + 주문웹앱 (2)
  - 이 원칙으로 11개 산출물이 **8개 챕터**가 된다.

### 챕터 순서

포지셔닝이 "혼자 끝까지"이므로 완결형 개인 작업이 앞선다. 이후 규모와 난이도로 실무를 받친다.

| # | 프로젝트 | 소속 | 역할 |
|---|---|---|---|
| 01 | BABAMBI | SELF-DIRECTED | 브랜딩 · SNS 콘텐츠 · 반응형웹 |
| 02 | GOYO | SELF-DIRECTED | 브랜딩 · 반응형웹 |
| 03 | 13 MONTHS | CLIENT | 브랜딩 · 카페24 커머스 |
| 04 | DITOLAND | UTPLUS | 웹 · 앱 · 메타버스 |
| 05 | ONT KIOSK | RTBS | 테이블 주문 키오스크 |
| 06 | NAVYZ KIOSK | RTBS | 레스토랑 키오스크 |
| 07 | TOM N TOMS | RTBS | 템플릿 주문웹 · 웹앱 |
| 08 | NZ SYSTEM | RTBS | 브랜드 사이트 |

## 3. 색 — 무채색만

작업물 썸네일이 유일한 채도다. UI에는 어떤 색상도 없다.

```css
--void-000:#000000;  /* 절대 배경 — 챕터 사이의 공백 */
--void-100:#08080A;  /* 기본 페이지 배경 */
--void-200:#101013;  /* 카드 · 패널 */
--void-300:#1A1A1F;  /* 상승면 */

--line-faint:rgba(255,255,255,.07);   /* 있는 줄 모르게 */
--line:rgba(255,255,255,.16);         /* 구조 분할 */
--line-strong:rgba(255,255,255,.44);  /* 강조 · 활성 */

--text-000:#FFFFFF;  /* 대형 타이포 */
--text-100:#E6E6E9;  /* 본문 */
--text-300:#8E8E97;  /* 보조 · 설명 */
--text-500:#55555D;  /* 메타 · 번호 */
--text-600:#34343A;  /* 현재가 아닌 좌표 눈금 — 읽히지 않는 것이 의도 */
```

- 이전 시스템의 **오렌지 `#FF4B1F`는 폐기**한다.
- 강조는 색이 아니라 **밝기와 여백**으로 만든다. 활성 상태 = `--text-300` → `--text-000`.
- 호버 시 썸네일의 채도/밝기를 올려(`filter`) 이미지가 스스로 빛나게 한다. UI에 색을 더하지 않는다.

## 4. 타이포 — 중성적 그로테스크

| 역할 | 서체 | 비고 |
|---|---|---|
| Display (영문 대형) | **Archivo** (variable, OFL) | Helvetica Now 계열의 담백한 그로테스크. 자기주장이 없어 9개 작업물을 모두 받아낸다 |
| Body (한글·영문 본문) | **Pretendard Variable** | 동적 서브셋 |
| Meta (번호·라벨·좌표) | **JetBrains Mono** | 소속·연도·인덱스 전용 |

- 이전 시스템의 **Tenada는 폐기**한다. 단일 웨이트라 위계를 쌓을 수 없다.
- 언어: **영문 구조 + 한글 본문.** 제목·네비·라벨은 영문 대문자, 설명과 서사는 한글.

```css
--fs-mega:clamp(48px,8vw,124px);    /* 프로젝트명 — TOM N TOMS 같은 긴 이름이 3줄로 넘어가지 않는 상한 */
--fs-h2:clamp(30px,4.4vw,68px);
--fs-h3:clamp(20px,2.2vw,30px);
--fs-body:clamp(15px,1.15vw,18px);
--fs-meta:12px;  /* mono, letter-spacing .12em, uppercase */
```

## 5. 여백 — 우주는 여백이다

작업물이 돋보이는 유일한 방법은 그 주변을 비우는 것이다.

- 챕터당 최소 높이 `100svh`. 한 화면에 프로젝트는 하나뿐.
- 좌우 마진: 데스크톱 `clamp(40px, 6vw, 120px)` / 모바일 `20px`.
- 8pt 배수 스페이싱. 섹션 상하 여백은 `clamp(96px, 14vh, 200px)`.
- 한 챕터 안의 텍스트는 **4줄을 넘기지 않는다.** 나머지는 상세 페이지로.

## 6. 모션 원칙

1. **모든 모션은 스크롤에 종속된다.** 자동 재생 루프는 히어로 파티클과 SCROLL 힌트뿐.
2. 이징: `cubic-bezier(.22,.8,.24,1)`. 지속: 진입 `600–900ms`, 상태 변화 `250–350ms`.
3. 관성 스크롤 Lenis `lerp 0.09`.
4. `prefers-reduced-motion`에서는 파티클·블랙홀·시차 전부 정지하고 정적 레이아웃으로 폴백.
5. **금지** — 회전하는 별, 반짝임, 시차 배경 여러 겹. 무거워지고 컨셉을 배신한다.

## 7. 반응형

| | 모바일 | 태블릿 | 데스크톱 |
|---|---|---|---|
| 기준 | ~767 | 768–1279 | 1280~ |
| 챕터 | 이미지 위 / 텍스트 아래 세로 적층 | 동일, 여백 확대 | 텍스트·이미지 좌우 분할 |
| 히어로 | 스케일 축소값 완화(.55) | — | .74 |

## 8. 미결 사항

- ONT 키오스크 커버 이미지 — 현재 프로젝트 내부 `intro.jpg`를 임시 사용 중. 정식 커버 필요.
- NAVYZ 상세 이미지 — 캡쳐 폴더 위치 확인 필요.
- ~~한국가이던스~~ — **결정됨**: 작업물 챕터로 올리지 않고 ABOUT 경력에만 표기한다.

---

# v2 — 2026 트렌드 반영 (인터뷰 3라운드)

트렌드 조사 결과 v1은 방향은 맞으나 안전한 쪽이었다. 아래 네 가지를 보강한다.
근거: 순수 블랙 다크모드의 정체성화, 초대형 플레인 산세리프, 키네틱 타이포,
CSS 스크롤 구동 애니메이션의 크로스브라우저 지원 완성, broken grid / 편집 감각.

## 9. 레이아웃 변주 — 3종 로테이션

여덟 화면이 같은 구성이면 한 화면을 여덟 번 보는 것과 같다. 시스템을 깨지 않는
선에서 세 개의 변주를 돌린다. `data-layout` 속성 하나로 전환된다.

| 변주 | 구성 | 쓰는 곳 |
|---|---|---|
| `a` | 좌 텍스트 / 우 이미지 | 기본 |
| `b` | 이미지가 화면 전체, 텍스트는 그 위 좌하단 + 스크림 | **분위기 있는 와이드 이미지에만** |
| `c` | 좌 이미지 / 우 텍스트 (미러) | 기본의 반전 |

배치: `A B C B A C A C` (바밤비 / 고요 / 열세달꽃 / 디토랜드 / ONT / NAVYZ / 탐탐 / NZ)

- **`b`는 절대 UI 스크린샷에 쓰지 않는다.** 키오스크·주문웹 화면을 풀블리드로
  크롭하면 실수처럼 보인다. 사진과 3D 씬에만.
- `b`는 스크림이 대비를 담당하므로 이미지 기본 감광을 `.84`로 완화한다(다른 변주는 `.66`).
- 1279px 이하에서는 세 변주 모두 같은 세로 적층으로 접힌다. 좁은 화면에서
  크롭된 이미지 위의 텍스트는 읽히지 않는다.

## 10. 모션 v2 — CSS 스크롤 타임라인

JS 옵저버를 전부 걷어내고 네이티브 `animation-timeline: view()`로 옮겼다.
메인 스레드를 쓰지 않고, 스크롤과 완벽히 동기화된다.

**전부 `@supports (animation-timeline: view())` 안에 정의하고, 기본 상태를 완료
상태로 둔다.** 미지원 브라우저는 정적인 완성 화면을 보게 되며 이는 열화가 아니라
올바른 폴백이다.

| 이름 | 대상 | 구간 | 내용 |
|---|---|---|---|
| `reveal` | `[data-reveal]` | `entry 6% → cover 22%` | 26px 상승 + 페이드 |
| `title-settle` | `.chapter-title` | `entry 18% → cover 46%` | **키네틱 타이포 (2축)** |
| `media-drift` | `.media-drift` | `cover 0% → 100%` | **시차** ±3.5% |

### 키네틱 타이포

챕터가 화면 중앙으로 올라올수록 제목의 자간이 벌어진 상태에서 조여지고, 가변폰트
무게가 500 → 700으로 실제 보간된다. 동시에 불투명도 .62 → 1.

```
진입 전 : letter-spacing  .015em / wght 500 / wdth  82 / opacity .62
중앙    : letter-spacing -.045em / wght 700 / wdth 100 / opacity 1
```

Archivo는 축이 **두 개**다. 무게(100–900)와 **너비(62–125%)**. 둘 다 쓴다 —
2026에 네오 그로테스크가 지배적인 이유가 정확히 이 다축 가변성이고, 자간만
움직이는 것보다 훨씬 독특하다.

**폰트를 요청할 때 두 축을 다 적어야 한다.**

```
family=Archivo:wdth,wght@75..112,400..700   ← 이렇게
family=Archivo:wght@400..700                ← 이러면 wdth가 조용히 무시된다
```

축 이름은 알파벳 순(wdth, wght)으로 적어야 하고, 두 번째 형태로 요청하면
Google Fonts가 너비가 고정된 인스턴스를 내려주기 때문에 CSS에 `wdth`를 써도
계산된 스타일에서 그냥 사라진다. 실제로 한 번 겪었다.

너비가 줄어도 제목의 박스 폭은 변하지 않는다(그리드 칼럼을 채우는 블록이라
글자만 안쪽에서 좁아진다). 레이아웃 이동이 없다.

### 깊이 (Z축)

`.media-drift` 래퍼가 시차 transform만 소유한다. 이미지 자신의 transform은 호버
확대가 쓰므로 둘을 분리하지 않으면 서로를 덮어쓴다. 이동폭은 ±3.5%로 의도적으로
작다 — 이것은 깊이의 힌트이지 패럴랙스 사이트가 아니다.
`b` 변주는 이미 화면을 가득 채우므로 시차를 끈다(가장자리가 드러난다).

## 11. 유지하기로 한 것 (트렌드상 폐기된 것들)

- **전체 패럴랙스 금지** — 2026 기준 사망. 우리는 이미지 하나에 ±3.5%만 쓴다.
- **캐러셀 금지** — 돌아오지 않는다.
- **글래스모피즘은 헤더에만** — 2026 권장 용법(네비게이션·플로팅 요소에 선택적)과
  일치. 전면 사용은 하지 않는다.

## 12. 확정, 구현 대기

- **다음 프로젝트 풀블리드 전환** — 상세 페이지 하단에서 다음 작업이 화면 가득
  등장해 이어보게 만드는 블록. 상세 페이지 틀이 정해지면 함께 구현한다.


## 13. 히어로 v2 — 시간 구동에서 스크롤 구동으로

**문제.** 기존 히어로는 시간 기반 자동 재생이었다. 페이지가 뜨면 보든 말든 약 2초짜리
시퀀스가 혼자 재생되고 끝났다. 선이 그어지고 직함이 한 글자씩 나타나는 꼬리 부분을
실제로 보는 사람이 거의 없다 — 로딩 중이거나 탭을 방금 전환했거나 딴 데 보고 있으면
그대로 놓치고, 한 번 놓치면 새로고침 말고는 다시 볼 방법도 없다.

**해결.** 자동 재생은 SOUL이 나타나는 첫 1.2초까지만 남기고, 그 뒤 전부를 스크롤에
묶었다. 스크롤해야 진행되므로 놓칠 수가 없다. 이미 블랙홀 이탈이 스크롤 구동이었으니
히어로 전체가 하나의 연속된 스크롤 서사가 된다.

```
자동 (약 1.2초)   입자 구름 → SOUL 실루엣 → SOUL 실체화
───────────  여기서부터 사용자가 통제  ───────────

[A단계] 히어로가 화면을 온전히 소유 — WORK가 아직 안 덮음
  pA 0.00 – 0.18   SO / UL가 벌어지며 Y 슬롯이 열림
  pA 0.15 – 0.36   Y 줄기 등장 → 옆으로 1회전하며 글자가 열림
  pA 0.36 – 0.45   SO / UL가 제자리로 좁혀짐
  pA 0.42 – 0.58   SOYUL · 선 · 직함이 한 덩어리로 솟아오름
  pA 0.58 – 1.00   ★ 정지 — 완성된 이름이 깨끗한 보이드 위에 그대로 머무름

[B단계] WORK가 히어로를 덮으며 올라옴
  pB 0.00 – 1.00   입자가 중앙으로 빨려들며 가속, 동시에
                   SOYUL이 축소·이동해 헤더 로고 자리에 착지
```

- **스크롤 길이는 2화면.** `--hero-span: 2` 하나가 유일한 출처다. hero.js가 이 값으로
  스페이서 높이를 잡고, site.js는 그 **측정된 스페이서 높이**를 읽는다. 토큰을 아는 곳이
  한 곳뿐이라 둘이 어긋날 수 없다.

- **두 단계의 경계는 계산하지 말고 유도한다.** 히어로가 `position:fixed`이므로 WORK
  시트는 스페이서가 끝날 때가 아니라 **스페이서에 한 화면이 남았을 때**부터 히어로를
  덮기 시작한다(`scrollY = 스페이서높이 − 뷰포트높이`). 진행도를 전체 스팬으로 나누면
  정지 구간이 이미 덮인 화면 위에서 일어난다 — 실제로 한 번 그렇게 만들었고, 이름이
  절반 가려진 채 쉬고 있었다. 이 경계로 A/B를 가르면 `--hero-span`을 어떻게 바꾸든
  정지 구간은 항상 덮이지 않은 온전한 화면 위에서 일어난다.
- **입자 가속** — 이탈 진행도의 **제곱**에 비례해 중앙으로 끌어당기고 감쇠를 낮춘다.
  초반에는 거의 미동이 없다가 끝에서 쏟아져 들어간다. 이것이 "빨려들어간다"는 감각을
  만든다. 선형으로 하면 그냥 흘러가는 것처럼 보인다.
- clip-path는 양쪽 끝을 **모두 polygon으로** 써야 보간된다. polygon에서 inset으로 가는
  전환은 보간할 대상이 없어 그냥 튄다.
- `prefers-reduced-motion`에서는 스크럽 전체를 건너뛰고 완성 상태를 그대로 보여준다.


## 14. 이름은 사라지지 않는다 — 헤더 로고로 착지

**문제.** 조립이 끝나는 지점(p .84)과 이탈이 시작되는 지점(p .82)이 겹쳐 있었다.
완성된 SOYUL이 온전히 보이는 구간이 사실상 0이었고, 완성 62px 뒤에는 이미 절반이
날아갔다 — 휠 두 칸. 이름을 보라고 만든 히어로인데 이름을 볼 시간이 없었다.

**해결.** 두 가지를 동시에 고쳤다.

1. **정지 구간** — 조립 완료부터 A단계 끝까지(약 290px, 0.42화면) 이름이 완전히
   정지한다. 좌표를 재보면 이 구간 내내 높이 64px, 중심 y 271px로 1px도 움직이지 않는다.
2. **착지** — 이름은 블랙홀에 빨려들지 않는다. 위로 올라가며 축소돼 **헤더 로고 자리에
   그대로 앉는다.** 무너지는 것은 보이드뿐이다. 우주는 무너지고 이름만 남는다.

```
정지 끝   높이 64px, 중심 (244, 271)
착지 중   높이 41px, 중심 (138, 148)
착지 완료 높이 18px, 중심  (48,  25)  ← 헤더 로고와 동일
```

- **착지 좌표는 측정한다, 계산하지 않는다.** `measureLanding()`이 헤더 로고의 실제
  `getBoundingClientRect()`를 읽어 목표를 잡는다. 하드코딩한 오프셋은 패딩이나 폰트
  크기가 바뀌는 순간 어긋난다. 헤더가 숨어 있을 때는 자기 높이만큼 위에 있으므로 그만큼
  보정한다.
- 스케일 기준점은 래퍼의 중심이 아니라 **SOYUL 자신의 중심**이어야 한다. 래퍼는 전체
  너비를 차지하므로 그 중심으로 축소하면 이름이 엉뚱한 곳으로 간다.
- 헤더의 진짜 로고는 `is-logo-hidden`으로 가려 두고, 착지 마지막 6%에서 교차 페이드로
  넘긴다. 같은 크기 같은 자리이므로 전환이 보이지 않는다. 두 개의 SOYUL이 동시에 보이면
  복제처럼 읽힌다.
- 헤더 자체는 착지 초반(pB 0.2)에 들어와 있어야 한다. 이름이 도착했는데 받을 자리가
  없으면 안 된다.
- **히어로를 숨기는 처리는 반드시 마지막에.** 먼저 숨기면 착지가 헤더에 닿기 한 프레임
  전에 얼어붙는다. 실제로 그렇게 만들었다가 이름이 24px에서 멈춰 있었다.

## 15. 좌측 레일과 인트로 — 겹침 사고에서 배운 것

**사고.** WORK 인트로의 제목 위로 좌표계 01–08이 겹쳐 찍혔다. 원인 두 가지:

1. 레일이 차지하는 74px 여백을 `.chapter`에만 확보하고 `.section-intro`에는 잊었다.
   같은 값을 두 곳에 하드코딩하고 세 번째 곳을 빠뜨린, 전형적인 방식이다.
2. 레일 활성 조건이 `#work` 전체 기준이라 챕터가 아직 안 나온 인트로 구간에서도 켜졌다.

**규칙.**

- 레일 폭은 `--rail: 74px` **토큰 하나**가 유일한 출처다. WORK 안의 모든 블록은
  `padding-left: calc(var(--margin) + var(--rail))`로 이 폭을 비운다. 1279px 이하에서는
  레일이 사라지므로 여백도 함께 회수한다.
- 레일은 **챕터에 속한다.** 활성 조건은 섹션이 아니라 첫 챕터의 top과 마지막 챕터의
  bottom 기준이다. 챕터가 화면에 없으면 레일도 없다.

**레일의 대비.** 현재 항목만 순백(`--text-000`), 나머지는 `--text-600: #34343A`.
나머지 눈금이 **읽히지 않는 것이 의도**다. 이전에는 전부 `--text-500`이라 여덟 개가
전부 시선을 끌었고, 그 결과 네비게이션이 아니라 노이즈로 읽혔다. 눈금은 척도이고,
읽어야 할 것은 지금 어디인가 하나뿐이다.

**인트로.** 제목과 설명문은 유지하되 `min-height: 100svh`로 챕터와 같은 리듬을 갖는다.
이전에는 화면 위쪽 60%에만 내용이 몰리고 아래가 비어, WORK가 시작되자마자 리듬이 한 번
끊겼다. 모든 챕터가 한 화면을 온전히 쓰는 사이트에서 인트로만 그러지 않을 이유가 없다.

## 16. WORK 인트로 카피 (확정)

> **혼자든 팀이든,**
> **끝까지 만듭니다.**
>
> 개인 작업에서는 컨셉·브랜딩·콘텐츠·웹을 혼자 끝냈고, 실무에서는 키오스크·주문웹·
> 메타버스까지 환경을 가리지 않았습니다. 둘을 같은 기준으로 만들었습니다.

**제목은 본문이 하는 말을 반복하지 않는다.** 제목이 두 축을 한 문장으로 압축해 주장하고,
본문이 각 축을 근거로 푼다. 이전 후보 중 "혼자 완결한 것과 팀에서 구현한 것"은 본문
첫 문장과 같은 말이라 같은 내용을 두 번 읽게 만들었다.

**안내문을 쓰지 않는다.** 이전 본문은 "여덟 개의 작업을 하나씩 봐주세요"로 끝났다.
밑에 여덟 개가 있으면 어차피 알게 된다. 그 자리에는 안내가 아니라 주장이 와야 한다 —
"둘을 같은 기준으로 만들었습니다"가 그 자리를 대신한다. 회사 일이라고 대충 하지
않았다는 말이고, 개인 작업과 실무를 한 흐름에 섞어 놓은 이유이기도 하다.

**쓸 수 없는 문장.** "여덟 개 모두 실제로 열리는 사이트입니다" 류는 사실이 아니다.
NAVYZ는 접근 가능한 주소가 없고 캡쳐만 있다. 포트폴리오 카피는 검증 가능한 것만 쓴다.

---

# 검수 1차 — 실측 결과와 조치

## 17. 대비 — 무채색 규율이 정보를 가리면 안 된다

측정해보니 `--text-500`이 **2.71:1**로 WCAG AA(4.5:1)에 크게 미달했다. 장식이었다면
넘어갈 수 있지만 이 색은 **정보**를 지고 있었다 — 챕터 메타의 소속과 연도,
경력 카드의 ROLE / PROJECT 라벨, 푸터. 11~12px에 2.71:1이면 실제로 읽히지 않는다.
채용 담당자가 "어느 회사에서 언제 한 일인지"를 못 읽는 것은 미학의 문제가 아니다.

```
--text-500  #55555D → #7C7C86    2.71:1 → 4.84:1  ✅
--text-300  #8E8E97 (유지)        6.16:1           ✅
--text-600  #34343A (유지)        1.62:1  — 의도된 비가독(좌표 눈금)
```

`--text-300`과 한 단계 차이는 유지하면서 AA를 넘겼다. **비가독을 의도할 수 있는 것은
정보를 지지 않은 요소뿐이다.**

## 18. 챕터 전체가 클릭 대상이다

썸네일이 링크가 아니었다. 클릭 가능한 것은 98×22px 텍스트 링크 두 개뿐인데
호버하면 이미지가 밝아졌다 — **반응할 것처럼 굴고 눌리지는 않는** 최악의 조합이었다.

앵커는 중첩할 수 없으므로 챕터를 `<a>`로 감싸는 대신 덮개를 쓴다.

```
.chapter        position:relative
.chapter-hit    position:absolute; inset:0; z-index:1   ← 아트워크 위, 링크 아래
.chapter-links  position:relative; z-index:2            ← 자기 클릭을 지킨다
```

덮개는 `tabindex="-1" aria-hidden="true"`다. 키보드와 스크린 리더는 눈에 보이는
"Case Study" 링크 하나만 만난다. 히트 테스트로 확인했다 — 이미지와 제목 위는
`chapter-hit`, 두 링크 위는 각자 자신.

## 19. 페이지는 끝까지 같은 밀도를 유지한다

검수 전에는 뒤로 갈수록 사이트가 죽었다.

| | 이전 | 이후 |
|---|---|---|
| 키네틱 타이포 | `.chapter-title`만 | `.section-title`까지 |
| CONTACT 높이 | 479px (반쪽) | 100svh |
| career 카드 호버 | 없음 | 배경 한 단계 상승 |
| ABOUT 제목 | 4줄로 깨짐 | 2줄 (`max-width` 22ch → 28ch, 문장 압축) |

**닫는 보이드.** CONTACT에 입자 필드가 돌아와 히어로와 수미가 맞는다. 단 히어로의
살아 있는 필드가 아니라 **한 번 그리고 끝나는** 정적 필드다(rAF 없음, 스크롤 연산 없음).
아무도 상호작용하지 않는 마지막 화면을 위해 두 번째 애니메이션 루프를 돌릴 이유가 없다.
밀도는 히어로보다 확실히 낮지만(면적당 1/3400), 그보다 더 성기면 필드가 아니라 먼지로
읽힌다.

---

# 상세 페이지 (인터뷰 4라운드 · 확정)

## 20. 케이스 스터디 템플릿

파일: `case.css` + `case.js`. `style.css`의 토큰을 그대로 쓰고, `site.js`는 **불러오지
않는다** — 그 파일은 홈의 히어로 스페이서를 전제로 만들어져 있어 상세 페이지에서 깨진다.
관성 스크롤과 헤더 높이는 `case.js`가 따로 잡는다.

```
오프닝     커버 풀스크린 + 제목 + 태그라인 + 선   (100svh)
브리프     4열 그리드 — 개요 / 역할 / 해결 / 기술
갤러리     비율 적응 스택 (+ 하위 프로젝트는 챕터로 구분)
다음 작업  풀블리드 전환
```

### 오프닝 — 스크롤 구동

커버가 화면을 채우고, 스크롤하면 제목 블록이 헤더 쪽으로 올라가며 커버가 뒤로 밀려나고
선이 왼쪽부터 그어진다. **시간이 아니라 스크롤이 진행시킨다.** 홈 히어로에서 고친 문제를
상세 페이지 여덟 장에서 반복할 이유가 없다. 히어로의 착지 모션과 같은 문법이기도 하다.

### 브리프 — 4열, 이미지보다 먼저

개요 / 내 역할 / 해결한 것 / 사용 기술. 각 2~4줄. ABOUT의 career 카드와 같은 패널 문법
(1px 간격 그리드)이라 사이트 전체가 한 시스템으로 읽힌다. 채용 담당자가 스크롤 없이
필요한 것을 다 본다. 1279px 이하 2열, 767px 이하 1열.

### 갤러리 — 비율 적응 스택

여덟 프로젝트의 자산 성격이 완전히 다르다. 그래서 배치를 **이미지에서 결정한다.**
`case.js`가 `naturalWidth / naturalHeight`를 재서 태그를 붙인다.

| 비율 | 태그 | 배치 |
|---|---|---|
| `< 0.42` | `long` | 전체 폭 + **자기 프레임 안에서 세로 스크롤** |
| `0.42 ~ 0.85` | `tall` | 반 폭 — 키오스크·모바일 화면이 짝을 이룸 |
| `0.85 ~ 1.45` | `square` | 반 폭 |
| `> 1.45` | `wide` | 전체 폭 |

- **페이지 전체 캡쳐는 줄여서 보여줄 이미지가 아니라 훑어야 할 문서다.** 축소하면
  아무것도 안 보인다. 자기 높이를 갖고 그 안에서 스크롤한다.
- 마크업의 `data-ratio`가 항상 계산을 이긴다. 기하학이 아니라 편집상의 이유로 어떤
  이미지를 크게 걸어야 할 때가 있다.
- **1장이든 20장이든 의도적으로 보여야 한다.** NAVYZ는 커버 한 장뿐인데, 이 원칙을
  어기고 "12장 그리드" 같은 걸 설계하면 그 페이지가 텅 빈다.

### 라이트박스

모든 shot이 클릭·Enter·Space로 열린다. UI 디테일은 원본 크기에서만 실력이 보인다.
`long` 이미지는 이미 자기 프레임에서 스크롤 중이므로 열지 않는다 — 독자가 하고 있는
스크롤과 싸우게 된다. Esc·배경 클릭으로 닫고, 닫을 때 `src`를 비워 긴 캡쳐가 메모리에
남지 않게 한다. 포커스는 열기 전 위치로 돌려준다.

### 하위 프로젝트

디토랜드(웹·앱·메타버스)와 탐앤탐스(주문웹·웹앱)는 **한 페이지 안에서 연속 챕터**로
나뉜다. `.case-chapter`가 구분선과 제목을 놓는다. 메인의 풀스크린 챕터와 같은 문법이라
사이트가 일관된다.

## 21. 상세 페이지 자산 현황

| 프로젝트 | 상태 |
|---|---|
| 바밤비 | ✅ 페이지 완성 — 소스 688장 중 16장 큐레이션 |
| 열세달꽃 / 고요 / NZ / ONT | 소스 폴더 있음 (333 / 284 / 55 / 55장) — 큐레이션 필요 |
| 디토랜드 | 정리된 8장 있음 |
| 탐앤탐스 | **0장** — 라이브 사이트 캡쳐 필요 |
| NAVYZ | **커버 1장** — 최소 구성으로 설계, 캡쳐 폴더 위치 확인 대기 |

## 22. 챕터 06 · 07 재정의 — 자산에서 역할을 추론하지 않는다

자산 폴더를 열어보고 두 챕터가 실제보다 작게 잡혀 있다는 것을 알았다. 그런데 **크게
잡는 방향도 틀렸다.**

### 키오스크 — 확인하지 않았다면 거짓말이 될 뻔했다

`D:\WORK - 알티비에스\03. 키오스크`에 type01~type12가 있고 캡쳐가 121장이라서,
"11종 템플릿을 세 폼팩터로 구현했다"로 쓰려 했다. 확인 결과 실제로는 **이미 12종이
구현되어 운영 중인 상태에서 입사해 유지보수를 맡고, 신규 페이지를 만들고, 타입별
컬러 배리에이션을 만든 것**이었다.

> **폴더 규모에서 역할 범위를 추론하지 않는다.** 자산이 많다는 것은 프로젝트가 컸다는
> 뜻이지 이 사람이 그 전부를 만들었다는 뜻이 아니다. 포트폴리오의 모든 문장은 면접에서
> 검증되고, 과장된 한 문장은 나머지 진짜 성과의 신뢰까지 함께 무너뜨린다.

확정된 서술: *"12종 템플릿이 이미 운영 중이던 키오스크 시스템에 합류해 유지보수를
맡았습니다. 신규 페이지를 만들고 타입별 컬러 배리에이션을 구축했으며, 세로·가로·모바일
세 폼팩터를 모두 다뤘습니다. NAVYZ가 그중 한 브랜드입니다."*

이어받아 확장한 일은 그대로 쓴다. 남이 만든 대규모 시스템의 구조를 읽고 그 문법에 맞춰
확장하는 것은 그 자체로 실무 역량이다.

### 주문웹 — TOM N TOMS → ORDER WEB

`독립형 주문웹 캡쳐`가 베이스 템플릿(type01, 68장)과 브랜드 배포(육대장 31, 인쌩맥주
31, bhc 7)로 나뉘어 있었다. 한 브랜드 이름이 **여러 프랜차이즈에 배포되는 템플릿
시스템**이라는 사실을 가리고 있었다. 챕터명을 `ORDER WEB`으로 바꾸고 탐앤탐스 모바일
웹앱을 같은 제품의 모바일 폼팩터로 이 챕터에 포함한다.

### 갱신된 챕터 목록

| # | 챕터 | id |
|---|---|---|
| 06 | KIOSK SYSTEM | `ch-kiosk` → `work-kiosk.html` |
| 07 | ORDER WEB | `ch-orderweb` → `work-orderweb.html` |

ABOUT의 경력 카드도 같은 서술로 맞췄다. 목록과 경력이 다른 이야기를 하면 안 된다.

## 23. 퍼블리셔의 근거 — Build 블록과 작업 목록

**문제 1.** 브리프의 Stack 칸이 `HTML · CSS · JavaScript · jQuery · Responsive`였다.
모든 퍼블리셔가 똑같이 쓰는 목록이라 근거가 없다. 게다가 바밤비의 Stack은 사실과 달랐다.

**문제 2 — 더 중요한 것.** 그래서 구현 설명을 문단으로 길게 써 넣었더니 사용자가
말했다: *"말이 너무 어려워. 난 개발자가 아니라 뭔말인지도 모르겠어. 내가 주력으로
하는건 html, css라서."*

> **본인이 설명하지 못하는 문장은 포트폴리오에 넣지 않는다.** 취향이 아니라 안전
> 문제다. 적힌 것은 면접에서 질문받고, 답하지 못하면 나머지 진짜 실력까지 의심받는다.
> 화려하지만 방어할 수 없는 문장보다 소박하지만 본인 것인 문장이 낫다.

### 확정된 형태

1. **브리프 4번째 칸은 Build.** 구축한 화면을 실제로 나열한다. 키오스크는
   "인트로 · 매장/포장 · 메뉴 목록 · 메뉴 상세 · 장바구니 · 테이블 번호 · 진동벨 ·
   팁 · 쿠폰 · 결제 · 영수증, 가로·세로·모바일 세 가지 화면으로 각각".
2. **Stack은 실제 쓴 것만, 쉬운 이름으로.** HTML·CSS를 앞에 둔다 — 그것이 주력이다.
3. **캡션은 짧은 한글로.** `장바구니 — 수량 변경과 삭제` 정도. 전문 용어를 넣지 않는다.
4. **작업 목록 섹션**(`.case-notes`, 라벨 `WHAT I BUILT`)은 **제목과 기술 이름만.**
   설명 문단을 넣지 않는다.

```
01  터치하면 버튼이 눌리는 효과      CSS Animation
02  구형 브라우저 대응              CSS 벤더 프리픽스
03  화면 안에 뜨는 가상 키보드       jQuery Keyboard
04  메뉴 목록 터치 스크롤            jQuery mCustomScrollbar
05  옵션 선택 (체크박스 · 라디오)     jQuery
06  타입별 컬러 배리에이션           CSS
```

### 내용은 소스 코드에서 뽑는다

지어내지 않는다. 실제 코드에서 확인한 것만 쓴다.

| 프로젝트 | 코드에서 확인한 것 |
|---|---|
| 키오스크 | `clickEffect` 키프레임 / `-ms-` 프리픽스 전면 사용 / `jquery.keyboard` / `mCustomScrollbar` / 옵션 토글 |
| 바밤비 | React · Vite · Motion / 스크롤 등장 효과, 열고 닫히는 모션 |
| 주문웹 | Swiper 2종 / `mCustomScrollbar` / 브랜드별 색상 교체 |

바밤비 Stack이 `HTML·CSS·Photoshop`으로 적혀 있었는데 실제로는 React·Vite·Motion
이었다. 추측으로 쓰면 **낮춰 쓰는 실수도 난다.**

## 24. TOOLS 섹션 — AI 활용을 근거로 만들기

`WHAT I BUILT` 아래에 `TOOLS` 섹션을 하나 더 둔다. 같은 `.note` 마크업을 재사용하므로
CSS 추가가 없다. **용도 → 도구** 형태로, 역시 제목과 이름만.

```
T1  제품 비주얼                ChatGPT · Flow
T2  랜딩페이지 코딩             Codex
T3  웹 시안 · 상세페이지 · 로고   Figma
T4  배너 · 프로모션 콘텐츠       미리캔버스
```

이것이 "혼자 끝까지 만든다"는 포지셔닝의 **실제 근거**다. 디자이너 혼자 브랜딩부터
코딩까지 어떻게 했는지를 도구 이름이 설명해 준다. 감추는 것보다 밝히는 쪽이 강하다 —
AI를 쓴다는 사실이 아니라 **무엇에 어떻게 배치했는지**가 역량이기 때문이다.

| 프로젝트 | 도구 |
|---|---|
| 바밤비 | 제품 비주얼 ChatGPT·Flow / 코딩 Codex / 시안·상세·로고 Figma / 배너 미리캔버스 |
| 고요 | 시안 Figma / 코딩 Codex |
| 열세달꽃 | 코딩 Codex / 비주얼 편집 Flow |

실무 프로젝트(키오스크 · 주문웹 · NZ)에는 TOOLS 섹션을 두지 않는다. 회사 환경에서
쓴 도구는 본인의 작업 방식이 아니다.

## 25. 작업 목록에 반드시 들어가야 하는 것

`WHAT I BUILT`는 다음을 빠짐없이 적는다. 제목은 쉬운 한글, 태그는 기술 이름.

- **프레임워크** (React, Vite 등)
- **HTML · CSS** — 주력이므로 반드시 명시
- **jQuery**
- **인터랙션** (슬라이더, 토글, 가상 키보드, 스크롤, 달력 등)
- **애니메이션 효과** (GSAP·ScrollTrigger, Motion, CSS Animation)

고요 예시 — 코드에서 확인한 것 전부:

```
01  스크롤에 맞춰 움직이는 애니메이션   GSAP · ScrollTrigger
02  화면에 들어오면 나타나는 효과       IntersectionObserver
03  예약 달력 (날짜 선택)             flatpickr
04  한국어 · 영어 번역 토글            JavaScript
05  맨 위로 가기 버튼                 IntersectionObserver
06  공통 헤더 · 푸터 분리              HTML · JavaScript
07  반응형 레이아웃                   HTML · CSS
```

## 26. 라이브 사이트 캡쳐 — 화면 크기 그대로

전면(full-page) 캡쳐를 시도했다가 접었다. **지연 로딩 페이지에서는 아래쪽이 빈 칸으로
찍힌다.** 열세달꽃 갤러리는 원본이 2500~4000px짜리 이미지 68장이라, 헤드리스가
디코딩을 기다리지 못하고 회색 자리만 남긴 채 촬영한다. 대기 시간을 늘려도, 캐시를
예열해도, 여덟 번을 재시도해도 결과가 들쭉날쭉했다.

**결론: 화면 크기 그대로(1440×900) 뜬다.** 첫 화면은 지연 로딩 대상이 아니므로 항상
선명하게 나오고, 모든 캡쳐가 같은 비율이라 나란히 놓았을 때 정돈되어 보인다.

```bash
CHROME="C:/Program Files/Google/Chrome/Application/chrome.exe"
# 데스크톱
"$CHROME" --headless=new --disable-gpu --hide-scrollbars   --virtual-time-budget=25000 --window-size=1440,900   --screenshot="main.png" "https://example.com/"
# 모바일 (2배 해상도로 떠야 갤러리에서 안 뭉갠다)
"$CHROME" --headless=new --disable-gpu --hide-scrollbars   --force-device-scale-factor=2 --virtual-time-budget=25000   --window-size=430,900 --screenshot="main-m.png" "https://example.com/"
```

- 첫 화면에 지연 로딩 이미지가 걸린 페이지는 **그 위까지만** 자른다. 열세달꽃 갤러리는
  570px(제목 + 카테고리 필터)로 잘랐다 — 그 페이지에서 직접 만든 부분이기도 하다.
- 1440×900 캡쳐는 비율이 1.6이라 자동 분류에서 `wide`가 되어 전체 폭을 먹는다.
  화면 캡쳐는 나란히 비교하는 게 목적이므로 `data-ratio="square"`로 **반폭 고정**한다.

## 27. 갤러리와 작업 목록 — 짧게, 균일하게

사용자 피드백: *"너무 페이지가 길어져. 글은 짧은데 계속 세로 스크롤이 생기잖아.
그냥 캡쳐화면 나열하는게 나을것같아. 굳이 설명 캡션 안달아도 될것같음"*

| | 이전 | 이후 |
|---|---|---|
| 캡션 | 이미지마다 설명 | **전부 제거** |
| 페이지 캡쳐 | 760px 스크롤 프레임 | 균일한 반폭 카드, 클릭 시 라이트박스 |
| 작업 목록 한 줄 | 전체 폭 + 24px 제목 | **2열 패널, 행 높이 64px** |
| 작업 목록 전체 | 1399px | **410px** |

**글이 짧으면 배치도 짧아야 한다.** 한 줄짜리 항목에 전체 폭과 큰 제목을 주면 여섯
항목이 화면 한 장 반이 된다. `.note-grid`는 브리프·경력 카드와 같은 1px 간격 패널
문법을 쓰므로 사이트 전체가 한 시스템으로 읽힌다.

## 28. 메인 페이지 인터랙션 정리

- **로고는 새로고침.** 헤더의 SOYUL은 `href="index.html"`이라 누르면 페이지가 다시
  로드되고 히어로 인트로부터 다시 본다. `#hero` 앵커였을 때는 스크롤만 올라가서
  이미 끝난 인트로를 다시 볼 방법이 없었다.
- **챕터 전체 링크는 폐지.** 이미지에만 링크를 건다(`.media-link`). 챕터 전체를
  덮으면 설명 텍스트를 선택할 수도, 드래그할 수도 없고 어디를 눌러도 이동해 버린다.
  이제 클릭 대상은 **이미지**와 **Case Study 링크** 둘뿐이고, 호버 밝아짐도
  `.chapter:hover`가 아니라 `.chapter-media:hover`로 옮겨 반응 범위와 클릭 범위가
  일치한다.
- **푸터는 바닥에.** `.contact`가 세로 가운데 정렬이라 푸터가 메일 주소와 함께 화면
  가운데로 떠 있었다. `.site-foot`에 `margin-top:auto`를 주어 바닥에 붙인다.
- **맨 위로 버튼.** `topbtn.js`가 마크업까지 만들어 붙이므로 아홉 개 HTML에 같은
  버튼을 복사할 필요가 없다. Lenis가 스크롤을 소유하고 있으므로 `window.__lenis`를
  통해 넘긴다 — 네이티브 `scrollTo`는 Lenis 루프와 싸운다. 한 화면 이상 내려가야
  나타난다.

## 29. 상세 페이지 8장 완료

| # | 챕터 | 파일 | 캡쳐 | 작업 목록 |
|---|---|---|---|---|
| 01 | BABAMBI | `work-babambi.html` | 15 | 5 + TOOLS 4 |
| 02 | GOYO | `work-goyo.html` | 16 | 7 + TOOLS 2 |
| 03 | 13 MONTHS | `work-13months.html` | 8 | 6 + TOOLS 2 |
| 04 | DITOLAND | `work-ditoland.html` | 11 | 5 |
| 05 | ONT KIOSK | `work-ont-kiosk.html` | 4 | 8 |
| 06 | KIOSK SYSTEM | `work-kiosk.html` | 12 | 6 |
| 07 | ORDER WEB | `work-orderweb.html` | 11 | 5 |
| 08 | NZ SYSTEM | `work-nz-system.html` | 7 | 5 |

### 마지막 세 장에서 확인한 사실

**ONT** — 현재 버전은 `menu-data.js`와 `kiosk.js` 두 개만 불러오는 **바닐라 구현**이다
(폴더에 jQuery·slick 등이 남아 있지만 그것은 `legacy-index.html` 쪽이라 쓰지 않았다).
화면은 `view-attract` · `view-menu` · `view-pay` · `view-done` 네 개가 한 페이지 안에서
전환된다. `data-lang`으로 한국어/영어를 바꾸고, 옵션 모달 · 장바구니 패널 · 토스트 ·
`orientationchange` 대응 · 대기화면 자동재생 영상이 있다.

**NZ SYSTEM** — 로컬 작업 폴더에 HTML 16장(메인 · 키오스크 · 온라인주문 · 매장안내 ·
문의 · 로그인 · 블로그 목록과 상세 6종 · 약관 · 개인정보). `components.js`가 `fetch`로
공통 요소를 불러오고, `IntersectionObserver` 두 곳이 스크롤 등장 효과를 담당한다.

**DITOLAND** — 회사 작업이라 소스가 없어 라이브 사이트를 캡쳐하고 정리된 앱 이미지
8장을 함께 썼다. 하위 경로(`/world`, `/room`)는 메인과 같은 화면을 반환해(SPA) 중복
캡쳐를 지웠다 — **캡쳐 후 해시를 비교해 같은 파일을 걸러내는 것이 필요하다.**

### 전체 점검

9개 페이지의 내부 링크를 전수 확인했고 끊어진 링크는 없다. 이미지 누락 0건,
가로 오버플로 0건. 마지막 NZ의 `case-next`는 01 BABAMBI로 돌아가 목록이 순환한다.
